name: Generate Rumbo

on:
#  schedule:
#    - cron: '17 5 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:  
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Run generator
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          SEND_NEWSLETTER: ${{ vars.SEND_NEWSLETTER }}
        run: node generate.js

      - name: Commit and push
        run: |
          git config user.name "rumbo-bot"
          git config user.email "bot@rumbo.wtf"
          git add index.html index-es.html index-no.html index-uk.html index-nl.html index-global-es.html index-spain-es.html index-norway-es.html index-uk-es.html index-nl-es.html all_output.json last_output.json recent_headlines.json
          git diff --staged --quiet || git commit -m "Daily update $(date -u +%Y-%m-%d)"
          git push --force
